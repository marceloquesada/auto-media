# Installation

To  install auto-media in a compatible , simply paste the following commands into the terminal.

```
sudo apt-get update &&
git clone https://github.com/marceloquesada/auto-media.git
docker compose up
```
